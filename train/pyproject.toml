[project]
name = "train"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "muon",
]

[tool.uv.sources]
muon = { git = "https://github.com/KellerJordan/Muon" }
